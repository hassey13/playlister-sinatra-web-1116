<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Songs!</title>
    </head>
    <body>
        <ol>
            <% @songs.each do |song| %>
                <li><a href="/songs/<%= song.slug %>"><%= song.name %></li>
            <% end %>
        </ol>

    </body>
</html>
